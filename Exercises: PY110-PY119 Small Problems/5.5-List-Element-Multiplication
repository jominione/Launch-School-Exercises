# Given two lists of integers of the same length, return a new list where
# each element is the product of the corresponding elements from the two
# lists.

# Solution:

def multiply_items(list1, list2):
    result = []
    for index in range(len(list1)):
        result.append(list1[index] * list2[index])
    
    return result

# Example:

list_a = [1, 2, 3]
list_b = [4, 5, 6]
print(multiply_items(list_a, list_b) == [4, 10, 18]) # True