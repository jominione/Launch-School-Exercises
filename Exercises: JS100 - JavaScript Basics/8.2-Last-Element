// Write a function that returns the last element of an input array. For 
// example:

console.log(last(['Earth', 'Moon', 'Mars'])); // 'Mars'

// Solution:

function last(input) {
  return input[input.length-1];
}